// lib/syncScheduler.ts
// 🔥 This is no longer needed since sync is real-time.
// We keep a stub so imports don’t break.

export function startSyncScheduler() {
  // No-op: real-time sync handles everything now
  console.info("Sync scheduler disabled — using real-time listeners instead.");
}
